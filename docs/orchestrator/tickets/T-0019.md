# T-0019 Provider Routing, Fallback, And Adapter Validation

Status: COMPLETED_VERIFIED
Date: 2026-07-25
Branch: orchestrator/v1-coding-sprint
Prior ticket commit: 060eed8

## Objective

Define and test provider selection and handoff while keeping Ollama/Qwen as the only required live provider.

## Scope Completed

- Added provider capability registry.
- Added health and availability states.
- Added provider transition validation.
- Reused provider-neutral checkpoint handoff from T-0015.
- Simulated rate limit and unavailability states.
- Prevented completed and failed tool calls from being repeated during switch.
- Added fake API provider configuration.
- Added fake browser provider configuration.
- Added disclosure policy enforcement for remote/browser providers.
- Kept credential values out of repository-facing config and logs.

## Files Added Or Modified

- `src/delegated/provider_router.rs`
- `src/delegated/mod.rs`
- `docs/orchestrator/PROVIDER_ROUTING.md`
- `docs/orchestrator/SPRINT_CHECKPOINTS.md`
- `docs/orchestrator/SPRINT_CREDITS.md`
- `docs/orchestrator/tickets/T-0019.md`

## Acceptance Matrix

| Criterion | Evidence |
| --- | --- |
| Simulated provider replacement succeeds | `provider_router::tests::simulated_provider_replacement_succeeds` |
| Completed tool calls are not repeated | `provider_router::tests::completed_tool_calls_are_not_repeated_during_switch` |
| All providers unavailable escalates | `provider_router::tests::all_providers_unavailable_escalates` |
| Browser/provider-specific state does not leak into journal schema | `provider_router::tests::browser_state_does_not_leak_into_handoff_schema` |
| Secrets remain outside repository and logs | `provider_router::tests::secrets_remain_as_env_var_names_only` |

## Verification

Focused tests:

```powershell
cargo test delegated::provider_router -- --nocapture
```

Final verification:

```powershell
cargo fmt --check
cargo clippy --all-targets --all-features -- -D warnings
cargo test
```

Results:

- `cargo fmt --check`: passed.
- `cargo test delegated::provider_router -- --nocapture`: passed, 6 tests.
- `cargo clippy --all-targets --all-features -- -D warnings`: passed.
- `cargo test`: passed, 171 tests and 1 ignored live Ollama smoke.

## Credit Use

Displayed credits used: unavailable in the local Codex interface.

Planning estimate: 80-130 credits.

## Distribution

Local commit only. Nothing is pushed, opened as a pull request, merged, released, deployed, or published.
