# T-0018 Run Coordinator, Approvals, Budgets, And Escalation

Status: COMPLETED_VERIFIED
Date: 2026-07-25
Branch: orchestrator/v1-coding-sprint
Prior ticket commit: 90ab774

## Objective

Coordinate high-level runs without mixing policy with provider-specific transport.

## Scope Completed

- Added contract readiness checks.
- Added run locking.
- Added turn, tool-call, and elapsed-time budget enforcement.
- Added approval decision validation using existing T-0013 approval binding rules.
- Added structured escalation packet construction.
- Added cancellation with lock release.
- Added final verification gate.
- Added final-review package with explicit `pushed=false` and `merged=false`.
- Kept provider transport out of the coordinator.

## Files Added Or Modified

- `src/delegated/coordinator.rs`
- `src/delegated/mod.rs`
- `docs/orchestrator/RUN_COORDINATOR.md`
- `docs/orchestrator/SPRINT_CHECKPOINTS.md`
- `docs/orchestrator/SPRINT_CREDITS.md`
- `docs/orchestrator/tickets/T-0018.md`

## Acceptance Matrix

| Criterion | Evidence |
| --- | --- |
| Ambiguity escalates with evidence | `coordinator::tests::ambiguity_escalates_with_evidence` |
| Budget exhaustion stops safely | `coordinator::tests::budget_exhaustion_stops_safely` |
| Stale approvals fail | `coordinator::tests::stale_approvals_fail` |
| Final completion requires verification | `coordinator::tests::final_completion_requires_verification` |
| No automatic push or merge occurs | `coordinator::tests::final_review_never_marks_push_or_merge` |

## Verification

Focused tests:

```powershell
cargo test delegated::coordinator -- --nocapture
```

Final verification:

```powershell
cargo fmt --check
cargo clippy --all-targets --all-features -- -D warnings
cargo test
```

Results:

- `cargo fmt --check`: passed.
- `cargo test delegated::coordinator -- --nocapture`: passed, 8 tests.
- `cargo clippy --all-targets --all-features -- -D warnings`: passed.
- `cargo test`: passed, 165 tests and 1 ignored live Ollama smoke.

## Credit Use

Displayed credits used: unavailable in the local Codex interface.

Planning estimate: 90-145 credits.

## Distribution

Local commit only. Nothing is pushed, opened as a pull request, merged, released, deployed, or published.
