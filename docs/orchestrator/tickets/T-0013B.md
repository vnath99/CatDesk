# T-0013B CatDesk Worker Runtime Architecture

Status: COMPLETED_VERIFIED
Date: 2026-07-25
Branch: orchestrator/v1-coding-sprint
Prior ticket commit: 35e23dc

## Objective

Freeze the CatDesk-owned, provider-neutral worker-loop design before persistence and runtime implementation.

## Scope Completed

- Replaced the v1 architecture decision with a CatDesk-owned model/tool loop.
- Kept OpenClaw as deferred optional research instead of a required runtime dependency.
- Defined conceptual runtime, provider, session, event sink, checkpoint, cancellation, and handoff boundaries.
- Defined normalized provider events and normalized tool-call validation.
- Mapped T-0013 events and state concepts into the CatDesk-owned runtime.
- Defined patch proposal, application result, comparison, and actual diff artifacts.
- Documented model-turn lifecycle, failure states, context efficiency, and local-versus-remote disclosure policy.

## Files Added Or Modified

- `docs/orchestrator/ARCHITECTURE_DECISION_WORKER_RUNTIME.md`
- `docs/orchestrator/MINIMAL_WORKER_RUNTIME.md`
- `docs/orchestrator/PATCH_AND_DIFF_PROTOCOL.md`
- `docs/orchestrator/EXECUTION_PROTOCOL_V1.md`
- `docs/orchestrator/SPRINT_CHECKPOINTS.md`
- `docs/orchestrator/SPRINT_CREDITS.md`
- `docs/orchestrator/tickets/T-0013B.md`

## Acceptance Matrix

| Criterion | Evidence |
| --- | --- |
| No required v1 contract assumes OpenClaw | Architecture decision removes OpenClaw from the critical path and preserves it only as deferred research. |
| Provider-specific details are behind one adapter boundary | `MINIMAL_WORKER_RUNTIME.md` defines `WorkerProvider`, `ProviderSessionV1`, `ProviderTurnRequestV1`, and normalized events. |
| Tool definitions are constructed solely by CatDesk | Architecture decision records the tool-surface invariant. |
| Patch lineage and actual diff evidence are explicit | `PATCH_AND_DIFF_PROTOCOL.md` defines proposal, application, comparison, and authoritative actual diff artifacts. |
| Network/context efficiency is defined | Runtime doc defines bounded context, artifact references, output caps, and compaction requirements. |
| Browser providers can be added later without changing journal or coordinator | Architecture decision and runtime docs keep browser providers behind the provider adapter boundary. |

## Verification

Focused review:

```powershell
git diff --stat HEAD
git diff --name-status HEAD
rg -n "OpenClaw owns|model/tool loop and worker transcript|OpenClaw is required" docs/orchestrator --glob "!**/T-0013B.md"
```

Final verification:

```powershell
cargo fmt --check
cargo clippy --all-targets --all-features -- -D warnings
cargo test
```

Results:

- `git diff --stat HEAD`: reviewed T-0013B documentation changes.
- `git diff --name-status HEAD`: reviewed T-0013B file set.
- `rg -n "OpenClaw owns|model/tool loop and worker transcript|OpenClaw is required" docs/orchestrator --glob "!**/T-0013B.md"`: no matches.
- `cargo test delegated -- --nocapture`: passed, 9 tests.
- `cargo fmt --check`: passed.
- `cargo clippy --all-targets --all-features -- -D warnings`: passed.
- `cargo test`: passed, 129 tests.

## Credit Use

Displayed credits used: unavailable in the local Codex interface.

Planning estimate: 45-75 credits.

## Distribution

Local commit only. Nothing is pushed, opened as a pull request, merged, released, deployed, or published.
