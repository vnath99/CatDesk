# T-0013 Architecture Decision and Execution/Event Protocol

Status: COMPLETED_VERIFIED
Date: 2026-07-25
Branch: orchestrator/v1-coding-sprint
Prior ticket commit: d0cc25e
Local ticket commit: recorded in Git history for this file

## Objective

Define component ownership and a typed v1 protocol for delegated runs, worker sessions, turns, items, tool calls, approvals, artifacts, escalation, final review, event ordering, and state transitions.

## Acceptance Matrix

| Criterion | Evidence |
| --- | --- |
| Invalid transitions fail deterministically | `state_machine::tests::invalid_and_terminal_transitions_fail` |
| Worker output cannot alter original objective | `contracts::tests::worker_output_cannot_change_immutable_objective` |
| Events have stable sequence ordering | `events::tests::events_validate_sequence_ordering` |
| Approvals are tied to one run and one request | `contracts::tests::approvals_are_tied_to_one_run_request_and_expiration` |
| Protocol can support restart and polling | append-only `event_sequence`, `EventCursor`, and `ProtocolHandshakeV1` |

## Files Added Or Modified

- `src/delegated/mod.rs`
- `src/delegated/contracts.rs`
- `src/delegated/events.rs`
- `src/delegated/state_machine.rs`
- `tests/fixtures/delegated/execution_contract_v1.json`
- `docs/orchestrator/EXECUTION_PROTOCOL_V1.md`
- `docs/orchestrator/tickets/T-0013.md`
- `src/main.rs`

## Verification

Focused tests:

```powershell
cargo test delegated -- --nocapture
```

Final verification:

```powershell
cargo fmt --check
cargo clippy --all-targets --all-features -- -D warnings
cargo test
```

Results:

- `cargo test delegated -- --nocapture`: passed, 9 tests
- `cargo fmt --check`: passed
- `cargo clippy --all-targets --all-features -- -D warnings`: passed
- `cargo test`: passed, 129 tests

## Credit Use

Observed credit usage is not exposed in the local Codex interface. Sprint ledger entry: **T-0013 actual displayed credits: unavailable; planning estimate: 60-95 credits**.

## Known Limitation

The protocol is implemented and tested locally, but integrated OpenClaw worker execution remains gated by the T-0012 finding: the installed OpenClaw CLI does not expose final worker-visible tool definitions before a model turn.

No dependency was added. Nothing was pushed, merged, published, released, or deployed.
