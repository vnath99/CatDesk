# T-0020 ChatGPT Supervisor MCP And Event Polling

Status: COMPLETED_VERIFIED
Date: 2026-07-25
Branch: orchestrator/v1-coding-sprint
Prior ticket commit: eaf85e5

## Objective

Define and test the ChatGPT supervisor-facing delegated-run tool surface.

## Scope Completed

- Added all 16 required supervisor tool names.
- Added local supervisor run record storage.
- Added ordered idempotent event polling.
- Added bounded artifact retrieval.
- Added bounded diff retrieval.
- Added patch lookup and parent/candidate comparison.
- Added checkpoint retrieval.
- Added escalation retrieval.
- Added one-shot escalation decision consumption.
- Added pause, resume, cancel, status, list, and final-review retrieval semantics.
- Kept large logs local through bounded responses.

## Files Added Or Modified

- `src/delegated/supervisor.rs`
- `src/delegated/mod.rs`
- `docs/orchestrator/SUPERVISOR_MCP_SURFACE.md`
- `docs/orchestrator/SPRINT_CHECKPOINTS.md`
- `docs/orchestrator/SPRINT_CREDITS.md`
- `docs/orchestrator/tickets/T-0020.md`

## Acceptance Matrix

| Criterion | Evidence |
| --- | --- |
| Ordered idempotent event polling | `supervisor::tests::ordered_event_polling_is_idempotent` |
| Bounded artifact and diff retrieval | `supervisor::tests::artifact_and_diff_retrieval_are_bounded` |
| Patch lineage inspection | `supervisor::tests::patch_lineage_can_be_inspected` |
| Escalation decisions cannot be reused | `supervisor::tests::escalation_decisions_cannot_be_reused` |
| Large logs stay local | Bounded artifact/diff responses mark truncation and `local_only=true`. |

## Verification

Focused tests:

```powershell
cargo test delegated::supervisor -- --nocapture
```

Final verification:

```powershell
cargo fmt --check
cargo clippy --all-targets --all-features -- -D warnings
cargo test
```

Results:

- `cargo fmt --check`: passed.
- `cargo test delegated::supervisor -- --nocapture`: passed, 7 tests.
- `cargo clippy --all-targets --all-features -- -D warnings`: passed.
- `cargo test`: passed, 178 tests and 1 ignored live Ollama smoke.

## Credit Use

Displayed credits used: unavailable in the local Codex interface.

Planning estimate: 65-105 credits.

## Distribution

Local commit only. Nothing is pushed, opened as a pull request, merged, released, deployed, or published.
